# production: trigger deploy (no functional change)
services:
  app:
    image: crewflow-app:latest
    ports:
      - "3001:3001"
      - "3002:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - ADMIN_USER=${ADMIN_USER}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - ADMIN_FORCE_PASSWORD=${ADMIN_FORCE_PASSWORD}
      - NODE_ENV=production
    container_name: crewflow-app-production
    restart: unless-stopped
    networks:
      - projetogran_crewflow-network
    depends_on:
      - postgres-prod

  postgres-prod:
    image: postgres:15
    container_name: postgres-prod
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - projetogran_crewflow-network

  pgadmin-production:
    image: dpage/pgadmin4:latest
    container_name: pgadmin-production
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
      - PGADMIN_SETUP_EMAIL=${PGADMIN_SETUP_EMAIL}
      - PGADMIN_SETUP_PASSWORD=${PGADMIN_SETUP_PASSWORD}
      - PGADMIN_CONFIG_SERVER_MODE=True
      - PGADMIN_CONFIG_PATH_PREFIX=/pgadmin
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    restart: unless-stopped
    networks:
      - projetogran_crewflow-network
    depends_on:
      - postgres-prod

networks:
  projetogran_crewflow-network:
    external: true

volumes:
  postgres_data:
  pgadmin_data:
